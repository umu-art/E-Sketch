apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: est-proxy-route
  namespace: e-sketch
spec:
  parentRefs:
    - name: e-sketch-gateway
      namespace: istio-system
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /proxy
      backendRefs:
        - name: est-proxy
          port: 80
