apiVersion: v1
kind: PersistentVolume
metadata:
  name: est-minio-pv
  labels:
    type: local
    app: est-minio
spec:
  storageClassName: manual
  capacity:
    storage: 16Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/data/est-minio-db"

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: est-minio-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 16Gi
