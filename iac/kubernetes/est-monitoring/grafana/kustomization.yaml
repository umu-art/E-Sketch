resources:
  - deployment.yaml
  - service.yaml
  - volume.yaml

configMapGenerator:
  - name: grafana-datasources-config
    files:
      - all.yaml

generatorOptions:
  labels:
    app: grafana
  disableNameSuffixHash: true