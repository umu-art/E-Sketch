---
apiVersion: v1
kind: Service
metadata:
  name: est-front
  namespace: e-sketch
spec:
  selector:
    app: est-front
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
