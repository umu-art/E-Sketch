apiVersion: v1
kind: Service
metadata:
  name: est-front
  namespace: est-units
spec:
  selector:
    app: est-front
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

